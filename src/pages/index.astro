---
export const prerender = true;

import AboutCards from "../components/AboutCards.astro";
import HomeHero from "../components/HomeHero.astro";
import HomeProjectsSection from "../components/HomeProjectsSection.astro";
import Certifications from "../components/Certifications.astro";
import HomeBlogSection from "../components/HomeBlogSection.astro";
import SiteFooter from "../components/SiteFooter.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { SITE } from "../lib/site";
import { projects } from "../data/projects";

const pageDescription =
  "Explore the portfolio of Dilukshan Niranjan, a Sri Lankan full-stack developer and AI engineer building scalable web apps, AI products, and performance-focused digital experiences.";
const homeKeywords = [
  ...SITE.keywords,
  "AI systems engineer",
  "full-stack portfolio",
  "web application developer",
  "software case studies",
];
const siteOrigin = Astro.site?.origin ?? Astro.url.origin;
const structuredData = [
  {
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: "Featured Projects",
    itemListElement: projects.slice(0, 6).map((project, index) => ({
      "@type": "ListItem",
      position: index + 1,
      url: new URL(project.route, siteOrigin).toString(),
      name: project.name,
      image: new URL(project.img, siteOrigin).toString(),
    })),
  },
];
---

<BaseLayout
  title={SITE.title}
  description={pageDescription}
  keywords={homeKeywords}
  pageType="profile"
  schemaType="ProfilePage"
  structuredData={structuredData}
>
  <main id="main-content">
    <HomeHero />
    <AboutCards />
    <HomeProjectsSection />
    <Certifications />
    <HomeBlogSection />
    <SiteFooter />
  </main>
</BaseLayout>
